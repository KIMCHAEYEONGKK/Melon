<p id="notice"><%= notice %></p>

<h1>Payusers</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @payusers.each do |payuser| %>
      <tr>
        <td><%= payuser.user_id %></td>
        <td><%= link_to 'Show', payuser %></td>
        <td><%= link_to 'Edit', edit_payuser_path(payuser) %></td>
        <td><%= link_to 'Destroy', payuser, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
 <% if @pay_t %>
	<p>asdasd</p>
<%end%>
<br>

<%= link_to 'New Payuser', new_payuser_path %>
