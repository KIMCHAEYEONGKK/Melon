<%= form_with(model: payuser,id: "payment_form", local: true ) do |form| %>

  <div class="field">
    <!-- <%= form.label :user_id %> -->
    <%= form.hidden_field :user_id , value: current_user.id%>
  </div>

  <div class="actions">
    <% form.submit %>
  </div>
<% end %>
