<%= form_with(model: play_list, local: true) do |form| %>
  <% if play_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(play_list.errors.count, "error") %> prohibited this play_list from being saved:</h2>

      <ul>
      <% play_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :track_id %>
    <%= form.text_field :track_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
